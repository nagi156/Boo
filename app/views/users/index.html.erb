<div class="container px-5 px-sm-0">
  <%= render 'layouts/errors', obj: @user %>
  <div class="row">
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'info',user: current_user %><!--部分テンプレート貼る-->
      <h2>New Book</h2>
      <%= render 'books/form', book: @book%>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <%= render 'index', users: @users %>
    </div>
  </div>
</div>